apiVersion: v1
kind: Service
metadata:
  name: dev-assist
  namespace: default
  labels:
    app: dev-assist
spec:
  type: ClusterIP
  selector:
    app: dev-assist
  ports:
    - name: web
      protocol: TCP
      port: 8080
      targetPort: 8080
